# 阻断项覆盖规则

1. S1 问题默认阻断交付，不得因为报告已经落盘就标记为通过。
2. 只有用户明确选择“立即修复”或“保留报告、稍后处理”时，主流程才可继续。
3. 覆盖必须记录：问题位置、用户选择、剩余风险和后续动作。
4. reviewer 缺失、调用失败、结果不完整或 JSON 损坏，不等于没有问题；指标状态保持 `unverified`。
5. 不允许主线为了满足 schema 自行编造 reviewer finding。可以使用 solo 审查，但必须注明不是独立 reviewer。
